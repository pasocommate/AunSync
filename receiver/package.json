{
  "name": "obs-delay-stream-receiver",
  "private": true,
  "version": "0.0.0",
  "description": "Build scripts for obs-delay-stream receiver assets",
  "scripts": {
    "build": "tsc --noEmit && vite build --logLevel warn"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "bulma": "^1.0.4",
    "i18next": "^26.0.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.8"
  }
}
